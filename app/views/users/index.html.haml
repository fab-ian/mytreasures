.box
  .box-header
    %h3.box-title Users #{ link_to 'add', new_user_path, class: 'btn btn-default btn-xs btn-flat' }

  .box-body.table-responsive.no-padding
    %table.table.table-hover
      %tr
        %th Name
        %th E-mail
        %th Language
        %th Function

      - users.each do |u|
        %tr
          %td= u.name
          %td= u.email
          %td= u.locale
          %td
            = link_to 'edit', edit_user_path(u)
